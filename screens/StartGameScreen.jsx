import { View, TextInput, Button } from "react-native";

const StartGameScreen = () => {
  return (
    <View>
      <TextInput />
      <Button title="Button" />
    </View>
  );
};

export default StartGameScreen;
